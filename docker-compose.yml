services:
  bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: bot_container
    volumes:
      - ./database:/app/database
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped

volumes:
  database_data:
    driver: local
